# from drawtree import draw_level_order
# import random
#
# lst = [''.join(random.sample('abcdef0123456789', 5)) for i in range(0, 100000)]
# lst.sort()
#
# draw_level_order(f'{[int(i, 16) for i in lst[:1000]]}')
